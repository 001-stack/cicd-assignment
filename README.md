# CI/CD Assignment

This is a fresh React + Vite project.

## Instructions
1. Install Node.js if you haven't already.
2. Run `npm install` to install dependencies.
3. Run `npm run dev` to start the development server.
